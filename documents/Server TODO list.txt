User
DTO 유틸 메소드 추가하여 사용할것(generateFileName, toEntity, fromEntity)
DTO가 id, password만 가지고 있으면 되는건지 확인(닉네임 등 다른 정보는 필요하지 않은지?)

Chat
DTO사용 구조로 변경(현재 Map이 사용됨)
DTO 유틸 메소드 추가하여 사용할것(generateFileName, toEntity, fromEntity)
미구현 함수 구현할것

공통
요청 처리에 실패했을 경우 null을 반환하면 클라이언트가 제대로 처리하지 못할 확률이 높음
->null 대신 명백한 responseEntity로 구현(테스팅 후 결정)